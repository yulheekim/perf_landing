import React, { Component } from 'react';
import { AppNavigator } from './config/route';

class Root extends Component {

    render() {
        return (
            <AppNavigator/>
        );
    }
    
};

export default (Root);